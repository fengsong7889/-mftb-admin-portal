name: Build & Push Backend Docker Image

on:
  push:
    branches: ['main']
    paths:
      - 'backend/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mftb-admin-backend:latest
            ghcr.io/${{ github.repository_owner }}/mftb-admin-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
