-- =====================================================================
-- 126: 驗收入庫批次明細增加驗收照片字段
--   1. photos — 驗收照片 JSON 數組，存 Base64 Data URL
--         通過項：到貨實物照片（收貨憑證）
--         不通過項：損壞/不符照片（退換貨證據）
-- 背景：驗收入庫需拍照留檔，通過時記錄到貨實物，不通過時記錄損壞或
--       與採購不符的物資照片，作為退換貨/索賠的依據。
-- =====================================================================

ALTER TABLE biz_eam_inbound_batch_item
    ADD COLUMN photos JSON DEFAULT NULL COMMENT '驗收照片JSON數組 [{name,dataUrl}]，通過=到貨照片，不通過=損壞/不符照片' AFTER reject_reason;
