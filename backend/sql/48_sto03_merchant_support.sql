-- ============================================================
-- 48: STO_03 銷量扶持 → 商家扶持，遷移至平台維度
-- 變更說明：
--   1. 名稱從「店鋪銷量扶持」改為「商家扶持」
--   2. 所屬維度從店鋪(2)遷移至平台(4)，屬於平台扶持行為
-- ============================================================

UPDATE `biz_organic_score_rule`
SET `name` = '商家扶持',
    `dimension` = 4,
    `updated_at` = NOW()
WHERE `rule_code` = 'STO_03'
  AND `deleted` = 0;
