-- =====================================================================
-- 153: 舊資產編號遷移至新格式
--   舊格式: FA20260916xxx 等不符合 {品牌}-{倉庫}-{分類}-{序號} 的編號
--   新格式: {品牌編碼}-{倉庫編碼}-{分類碼}-{4位序號}
--   示例: TB-ZH-0101-0001（閃蜂-珠海倉庫-手機分類-第1台）
-- 注意：
--   - 已符合新格式（XX-XX-XXXX-XXXX）的編號不影響
--   - 序號按 (品牌, 倉庫, 分類) 分組，依 id 升序排列
--   - 冪等安全：重複執行不會覆蓋已遷移的編號
--   - 實際遷移邏輯由 DataInitializer.migrateOldAssetNo() 在啟動時執行
--     （location 樹遍歷在 Java 中實現，比純 SQL 更可靠）
-- =====================================================================

-- 本腳本為參考文檔，實際遷移由 DataInitializer 自動執行。
-- 若需手動執行，請在 MySQL 8.0+ 中按以下步驟操作：
--
-- 1. 建立 location → 頂級倉庫 映射臨時表
-- 2. 計算新編號並寫入臨時表
-- 3. 從臨時表更新 biz_eam_asset
--
-- Java 啟動時自動遷移，通常無需手動執行。

SELECT 1;
