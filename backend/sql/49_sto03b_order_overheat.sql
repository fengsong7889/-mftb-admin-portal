-- ============================================================
-- 49: STO_03B 當天訂單超量扣分 → 訂單過熱調控，遷移至平台維度
-- 變更說明：
--   1. 名稱從「當天訂單超量扣分」改為「訂單過熱調控」
--   2. 所屬維度從店鋪(2)遷移至平台(4)，屬於平台流量均衡調控行為
--   3. 描述更新：平衡流量分配給其他商家機會
-- ============================================================

UPDATE `biz_organic_score_rule`
SET `name` = '訂單過熱調控',
    `dimension` = 4,
    `description` = '按當天計算，商家訂單過熱時按梯度降權，平衡流量分配給其他商家機會',
    `updated_at` = NOW()
WHERE `rule_code` = 'STO_03B'
  AND `deleted` = 0;
